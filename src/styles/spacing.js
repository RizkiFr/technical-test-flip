import { PixelRatio } from 'react-native';

export const spacing = {
    xxs: PixelRatio.getPixelSizeForLayoutSize(2),
    xs: PixelRatio.getPixelSizeForLayoutSize(4),
    s: PixelRatio.getPixelSizeForLayoutSize(8),
    m: PixelRatio.getPixelSizeForLayoutSize(12),
    l: PixelRatio.getPixelSizeForLayoutSize(20),
    xl: PixelRatio.getPixelSizeForLayoutSize(32),
};